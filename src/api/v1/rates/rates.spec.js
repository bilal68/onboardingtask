/* eslint-disable arrow-body-style */

describe('Test users', () => {
  beforeEach(() => { });

  afterEach(() => { });

  it('TODO: should do unit test for users', () => {
    expect(1 + 2).toBe(3);
  });

});
