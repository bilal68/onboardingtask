/* eslint-disable arrow-body-style */

describe('Test frequency', () => {
  beforeEach(() => { });

  afterEach(() => { });

  it('TODO: should do unit test for frequency api', () => {
    expect(1 + 2).toBe(3);
  });

});
